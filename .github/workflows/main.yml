name: Build APK gugugaga
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Dong goi LOVE Android
        uses: itsmevajn/love-android-actions@v1
        with:
          title: "Dai Dao Vo Bien"
          package: "com.tutien.daidaovobien"
